/* eslint-disable no-unused-vars */
function toObject(keyValuePair) { // arg is an [] length of 2
  var answer = {};

  answer[keyValuePair[0]] = keyValuePair[1];

  return answer; // returns an object {}
}
